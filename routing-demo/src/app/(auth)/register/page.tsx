export default async function Register() {

    // adding () make next consider it as route groups hence register can be accessed without auth in the path
    return (
        <h1>
            Register (example for route groups)
        </h1>
    )
}